# AGENTS.md — 多部门协同工具 Codex 项目指令模板

## 项目目标
本仓库用于开发“公司开发过程中的多部门协同工具”MVP。核心闭环是：需求提交 -> 需求评审 -> 项目立项 -> 任务执行 -> 会议待办 -> 风险/变更 -> 缺陷处理 -> 上线 checklist -> 消息/审计/看板。

## 工作原则
- 每次任务开始前先阅读 README、PRD/产品文档、package.json/配置文件、已有目录结构和相关模块代码。
- 不要盲目重写既有架构；优先按当前技术栈和代码风格补齐功能。
- 后端必须做权限和状态机校验，前端不能作为唯一校验来源。
- 前端按钮必须根据后端返回的 permissions/availableActions 展示。
- 所有新增、编辑、审批、状态变更、删除、权限变更动作必须写入 AuditLog。
- 关键业务对象接口使用统一响应：{ code, message, data, traceId }。
- 列表接口必须分页；无权限对象不能返回标题或敏感字段。
- 不新增真实密钥、真实账号、真实客户数据；测试数据必须标记为 demo/seed。

## MVP 优先级
1. 需求池、需求表单、需求详情、需求状态机。
2. 评审审批、流程节点、通知、待办。
3. 项目空间、任务看板、任务详情、阻塞和依赖。
4. 会议纪要、行动项转任务、评论附件、审计日志。
5. 风险台账、变更申请、影响评估。
6. 上线计划、上线 checklist、缺陷管理。
7. 首页工作台、消息中心、数据看板 Lite、全局搜索。
8. 权限配置、流程模板、测试、部署和文档。

## 验证要求
每次完成代码修改后，尽量运行项目已有的：
- lint
- typecheck
- unit tests
- integration/e2e tests
- build

如果某个命令不存在，请说明不存在，不要伪造测试结果。

## 输出要求
每次任务结束请输出：
- 修改文件清单
- 核心实现说明
- 运行过的命令和结果
- 未完成事项或风险
- 建议下一步
